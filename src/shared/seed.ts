// Seedless start: first run creates an empty default canvas — the canvas is
// populated from live sources via the picker, never from hardcoded content.
// This module survives only as the default canvas name.
export const SEED_CANVAS_NAME = "portfolio";
