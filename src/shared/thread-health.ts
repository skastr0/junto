/**
 * Thread health. The schemas and words live in the wire module (effect-only,
 * shared with the phone companion); this path keeps every desktop import
 * working.
 */
export * from "./wire/thread-health";
