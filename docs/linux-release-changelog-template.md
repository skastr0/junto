# Junto Linux Station Beta candidate

STATUS: BETA CANDIDATE TEMPLATE — NOT AUTHORIZED FOR PUBLICATION.

Before signing, the human release authority replaces this file with exact
versioned changes, compatibility notes, known limitations, migration impact,
and the source revision named by `source-revision.json`.

The signed changelog must not contain private host names, local paths, tokens,
canvas content, private keys, or claims unsupported by the release receipts.
